spring:
  config:
    import: security-variables.yml
  application:
    name: Memo
  thymeleaf:
    cache: false
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/sudalstagram?serverTimezone=Asia/Seoul
    username: root
    password: ${mysql.password}
  mvc:
    static-path-pattern: /static/**

    mybatis:
      mapper-locations: mappers/*Mapper.xml
      configuration:
        map-underscore-to-camel-case: true